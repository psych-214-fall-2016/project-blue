# Project modules and scripts

To put code into the Python path:

    pip3 install --user .

To run tests:

* install `pytest` with ``pip3 install --user pytest``;
* run tests with:

    py.test fmri_utils
